class Solution:
    def maxDepth(self, s: str) -> int:
        c=0
        a=0
        for i in s:
            if i=='(':
                c+=1
                a=max(a,c)
            elif i == ')':
                c -= 1
        return(a)
            

